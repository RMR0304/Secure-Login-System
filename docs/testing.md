# Security Testing & Verification Matrix

This matrix provides comprehensive evidence and test execution logs for both the mandatory HCLTech assessment scenarios (TC-01 through TC-05) and extended defensive security test cases (TC-06 through TC-20).

## Automated Test Case Verification Table

| ID | Test Scenario | Preconditions | Steps | Expected Result | Actual Result | Status | Observation |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01** | Correct password authentication | Student user `login_test@example.local` registered in database. | 1. Submit `POST /api/auth/login` with correct password `CorrectPassword!123`. | Login succeeds (HTTP 200), HttpOnly cookie `campus_session` is issued, failed attempt counter reset. | HTTP 200 OK. Cookie set. Welcome payload returned. | **PASS** | Authentication state verified in database; `last_login_at` updated. |
| **TC-02** | Incorrect password rejected | Student user registered in database. | 1. Submit `POST /api/auth/login` with incorrect password `WrongPassword!999`. | Login rejected (HTTP 401), failed attempts counter incremented, generic message returned. | HTTP 401 Unauthorized. Failed attempts = 1. Generic error message. | **PASS** | No indication given of whether email exists or password was mismatched. |
| **TC-03** | Same password, two users | Two accounts registered with identical password: `SecureCampusPassword!123`. | 1. Register User A (`STU001`).<br/>2. Register User B (`STU002`).<br/>3. Inspect SQLite `users` table. | Stored hashes differ completely due to unique per-user 16-byte random salts. | `user_a.hash != user_b.hash`. Both start with `$argon2id$v=19$`. | **PASS** | Confirms per-user dynamic salt generation eliminates rainbow table vulnerabilities. |
| **TC-04** | Reuse old password from history | User with active password history. | 1. Register with Password A.<br/>2. Change to B, then C, then D.<br/>3. Attempt to change back to Password A. | Change rejected (HTTP 400); system prevents reuse of passwords in last 3 history. | HTTP 400 Bad Request. Detail: `"New password cannot match your current password or your last 3 passwords."` | **PASS** | Validated via automated test `test_tc04_and_tc13_password_history_last_three_enforcement`. |
| **TC-05** | Repeated failures trigger account lockout | Active account `lockout_victim@example.local`. | 1. Submit 4 incorrect passwords.<br/>2. Submit 5th incorrect password.<br/>3. Submit correct password while locked. | Account locked on 5th attempt (HTTP 423). Subsequent attempts with correct password rejected during 15m window. | HTTP 423 Locked on 5th failure. Correct password rejected while locked. Lockout expires cleanly. | **PASS** | Mitigates automated brute-force attacks and credential stuffing bots. |
| **TC-06** | Weak password registration rejected | Portal running with password complexity policy active. | 1. Submit registration with passwords violating length, missing uppercase, lowercase, digits, or symbols. | Registration rejected (HTTP 400) with detailed policy violation messages. | HTTP 400 Bad Request across all test permutations. | **PASS** | Enforces minimum 12 chars, upper, lower, digit, symbol, and identity exclusion. |
| **TC-07** | Password and confirmation mismatch | Registration endpoint active. | 1. Submit `password != confirm_password`. | Registration rejected (HTTP 400). | HTTP 400 Bad Request. Detail: `"Passwords do not match."` | **PASS** | Enforces user confirmation parity before hashing occurs. |
| **TC-08** | Duplicate email registration rejected | Account with `unique@example.local` exists. | 1. Submit registration with identical email but new student ID. | Registration rejected (HTTP 409 Conflict). | HTTP 409 Conflict. Detail: `"An account with this email address already exists."` | **PASS** | Preserves email uniqueness constraint in SQLite. |
| **TC-09** | Duplicate student ID rejected | Account with `STU5001` exists. | 1. Submit registration with duplicate student ID but unique email. | Registration rejected (HTTP 409 Conflict). | HTTP 409 Conflict. Detail: `"An account with this Student ID already exists."` | **PASS** | Preserves unique institutional student identifier constraint. |
| **TC-10** | Access dashboard without authentication | Clean client with no session cookie. | 1. Request `GET /dashboard`.<br/>2. Request `GET /api/me`. | Browser request redirected (HTTP 302) to `/login`. API request rejected (HTTP 401). | HTTP 302 Redirect to `/login?unauthorized=1`. API returns HTTP 401. | **PASS** | Strict route-level authorization enforced by middleware. |
| **TC-11** | Change password without authentication | Unauthenticated client. | 1. Submit `POST /api/auth/change-password`. | Rejected with HTTP 401 Unauthorized. | HTTP 401 Unauthorized. | **PASS** | Credential modifications require validated server-side session. |
| **TC-12** | Wrong current password during change | Authenticated user session. | 1. Submit password change with mismatched current password. | Operation rejected (HTTP 400). | HTTP 400 Bad Request. Detail: `"Current password is incorrect."` | **PASS** | Protects against unauthorized changes if an unattended session is accessed. |
| **TC-13** | FIFO maintenance of password history | User changes password through 5 cycles (A &rarr; B &rarr; C &rarr; D &rarr; E). | 1. Verify history count capped at 3.<br/>2. Attempt Password A after it is pruned. | Password A succeeds after being pruned from last 3 history (FIFO queue verified). | History table holds exactly 3 Argon2id hashes. Password A accepted once pruned. | **PASS** | Validates exact implementation of "last-three password history". |
| **TC-14** | Logout session invalidation | Authenticated session with cookie. | 1. Call `POST /api/auth/logout`. | Session revoked in database (`revoked_at` set); cookie deleted. | HTTP 200 OK. Cookie deleted. Database entry marked revoked. | **PASS** | Server-side invalidation prevents session reuse. |
| **TC-15** | Access dashboard after logout | Client with revoked session token. | 1. Request `GET /dashboard` using post-logout cookie. | Access denied (HTTP 302 to `/login`). | HTTP 302 Redirected. `GET /api/me` returns HTTP 401. | **PASS** | Defeats session replay attacks. |
| **TC-16** | Expired recovery token rejected | User initiated recovery token. | 1. Token expiration timestamp passed.<br/>2. Submit reset request. | Rejected with HTTP 400. | HTTP 400 Bad Request. Detail: `"Recovery token has expired."` | **PASS** | Strict 10-minute token window verified. |
| **TC-17** | Recovery token reused rejected | User successfully consumed recovery token. | 1. Attempt second reset request using same token. | Rejected with HTTP 400 (Single-use enforcement). | HTTP 400 Bad Request. Detail: `"Recovery token has already been used."` | **PASS** | Single-use constraint enforced by `used_at` timestamp. |
| **TC-18** | Malformed request validation | Any endpoint. | 1. Submit invalid JSON or omitted mandatory fields. | Defensive validation error (HTTP 400); no stack traces. | HTTP 400 Bad Request. Clean sanitized error summary. | **PASS** | Pydantic exception handler prevents internal implementation disclosure. |
| **TC-19** | Zero sensitive data logged | All authentication endpoints exercised. | 1. Inspect `security_events` table and log output. | Plaintext passwords, raw tokens, and sensitive keys never logged. | Verified: Zero occurrences of passwords or raw tokens in database or logs. | **PASS** | Sanitizer redacts sensitive parameter values. |
| **TC-20** | Environment secret missing failure | Config initialization. | 1. Instantiate `Settings(SESSION_SECRET="")`. | Application fails safely with validation error, without disclosing secret values. | Pydantic `ValidationError` raised cleanly. | **PASS** | Defensive startup validation prevents operation with insecure default keys. |
